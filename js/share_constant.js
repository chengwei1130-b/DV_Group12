const width = 900;   // total width of SVG
const height = 400;  // total height of SVG
const margin = {top: 40, right: 20, bottom: 50, left: 70}; // margins around chart
const innerWidth = width - margin.left - margin.right;     // usable width inside margins
const innerHeight = height - margin.top - margin.bottom;   // usable height inside margins
